# Deploy to Cloudflare Pages Pipeline
defaults:
  run:
    shell: bash

name: Deploy to Cloudflare Pages
on:
  push:
    branches:  [ main ]
  pull_request:
    branches: [ main ]



jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
